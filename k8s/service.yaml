apiVersion: v1
kind: Service
metadata:
  name: rag-api-service
  namespace: rag-api
  labels:
    app: rag-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: rag-api