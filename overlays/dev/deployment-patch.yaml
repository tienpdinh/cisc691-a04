apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-pipeline
spec:
  template:
    spec:
      containers:
      - name: rag-pipeline
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"