apiVersion: apps/v1
kind: Deployment
metadata:
  name: rag-pipeline
spec:
  template:
    spec:
      containers:
      - name: rag-pipeline
        resources:
          requests:
            memory: "2Gi"
            cpu: "250m"
          limits:
            memory: "4Gi"
            cpu: "2000m"